import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  table: {
    minWidth: 650,
  },
  highPriority: {
    backgroundColor: "#fa4141",
  },
  mediumPriority: {
    backgroundColor: "#fcc203",
  },
  lowPriority: {
    backgroundColor: "#04db77",
  },
}));
